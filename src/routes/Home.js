import React from 'react';

import CommonWrapper from 'containers/CommonWrapper';

const Home = (props = {}) => {
  return (
    <div className={'Home'}>
      <CommonWrapper>Home</CommonWrapper>
    </div>
  );
};

export default Home;
